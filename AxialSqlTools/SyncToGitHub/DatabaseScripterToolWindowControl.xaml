<UserControl x:Class="AxialSqlTools.DatabaseScripterToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="600"
             Name="MyToolWindow">
    <Grid Margin="0">
        <StackPanel Orientation="Vertical" VerticalAlignment="Top" Margin="0">
            <StackPanel Orientation="Horizontal" Background="#FFB4B4B4">
                <TextBlock Margin="5" HorizontalAlignment="Center" VerticalAlignment="Top"
                           FontWeight="Bold" FontSize="18">
                    Sync to GitHub
                </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Vertical" VerticalAlignment="Top" Margin="5">
                
                <GroupBox Margin="0,5">
                    <GroupBox.Header>
                        <TextBlock Text="Target Repo" FontSize="14" FontWeight="Bold"/>
                    </GroupBox.Header>
                    <StackPanel Orientation="Horizontal" Margin="0,4">
                        <ComboBox Name="ReposComboBox"
                                  Width="300"
                                  ItemsSource="{Binding _repos}"
                                  DisplayMemberPath="DisplayName"
                                  SelectedItem="{Binding SelectedRepo, Mode=TwoWay}"
                                  Loaded="ReposComboBox_Loaded"/>
                        <StackPanel Orientation="Horizontal" Margin="8,0,0,0">
                            <Button Name="AddRepoButton"
                                    Content="+ Add" Width="60"
                                    Click="AddRepoButton_Click"/>
                            <Button Name="RemoveRepoButton"
                                    Content="– Remove" Width="60" Margin="5,0,0,0"
                                    Click="RemoveRepoButton_Click"/>
                        </StackPanel>
                        <Button Name="OpenRepoCommitsButton"
                                Content="Open repo commits"
                                Width="140" Margin="8,0,0,0"
                                Click="OpenRepoCommitsButton_Click"/>
                    </StackPanel>
                </GroupBox>
                
                <GroupBox Margin="0,5">
                    <GroupBox.Header>
                        <TextBlock Text="Source Databases (add from Object Explorer)" FontSize="14" FontWeight="Bold"/>
                    </GroupBox.Header>
                    <StackPanel>
                        <TextBlock Text="Database Connections:" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <ListBox Name="ConnectionsListBox"
                                     Width="400" Height="100"
                                     DisplayMemberPath="DisplayName"
                                     SelectedItem="{Binding SelectedConnection, Mode=TwoWay}"/>
                            <StackPanel Orientation="Vertical" Margin="8,0,0,0">
                                <Button Name="AddConnectionButton"
                                        Content="+ Add" Width="60"
                                        Click="AddConnectionButton_Click"/>
                                <Button Name="RemoveConnectionButton"
                                        Content="– Remove" Width="60" Margin="0,4,0,0"
                                        Click="RemoveConnectionButton_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>                

                <GroupBox Margin="0,5">
                    <GroupBox.Header>
                        <TextBlock Text="Commit" FontSize="14" FontWeight="Bold"/>
                    </GroupBox.Header>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,8">
                            <TextBlock Text="Commit Msg:" Width="80"/>
                            <TextBox Name="MessageTextBox" Width="300"
                                     Text="Add or update scripted objects"/>
                            <CheckBox Name="ConfirmBeforePushing"
                                      Content="Confirm before pushing"
                                      Margin="10,0,0,0"/>
                        </StackPanel>
                        <Button Name="RunButton"
                                Content="Script And Commit"
                                Width="160" Height="40"
                                HorizontalAlignment="Left"
                                Click="RunButton_Click"/>
                        <ProgressBar Name="OverallProgressBar"
                                     Height="16" Minimum="0" Maximum="100"
                                     Margin="0,8"/>
                        <ListBox Name="ProgressListBox" Height="200"/>
                    </StackPanel>
                </GroupBox>

            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
