<UserControl x:Class="AxialSqlTools.SchemaCompareWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" MinWidth="900" MinHeight="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 
        <DockPanel Margin="8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Source..." Width="90" Command="{Binding PickSourceCommand}"/>
                <TextBlock VerticalAlignment="Center" Text="{Binding SourceDisplay}" Width="280" TextTrimming="CharacterEllipsis"/>
                <Button Content="Target..." Width="90" Command="{Binding PickTargetCommand}"/>
                <TextBlock VerticalAlignment="Center" Text="{Binding TargetDisplay}" Width="280" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <CheckBox Content="Ignore Whitespace" IsChecked="{Binding Options.IgnoreWhitespace}"/>
                <CheckBox Content="Ignore Column Order" IsChecked="{Binding Options.IgnoreColumnOrder}"/>
                <CheckBox Content="Include Data" IsChecked="{Binding IncludeData}"/>
                <Button Content="Compare" Width="100" Command="{Binding CompareCommand}" />
            </StackPanel>
        </DockPanel>

   
        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <TreeView Grid.Column="0" ItemsSource="{Binding Results}" SelectedItemChanged="Tree_SelectedItemChanged">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type DiffuseMaterial}" ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsIncluded, Mode=TwoWay}"/>
                            <TextBlock Text="{Binding Status}" FontWeight="Bold" Width="80"/>
                            <TextBlock Text="{Binding ObjectType}" Width="80"/>
                            <TextBlock Text="{Binding Name}" />
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <GroupBox Header="Source Definition">
                    <TextBox Text="{Binding SelectedSourceDefinition}" FontFamily="Consolas" FontSize="12"
                   IsReadOnly="True" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto"
                   VerticalScrollBarVisibility="Auto"/>
                </GroupBox>
                <GroupBox Grid.Row="1" Header="Target Definition">
                    <TextBox Text="{Binding SelectedTargetDefinition}" FontFamily="Consolas" FontSize="12"
                   IsReadOnly="True" TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto"
                   VerticalScrollBarVisibility="Auto"/>
                </GroupBox>
            </Grid>
        </Grid>


        <DockPanel Grid.Row="2" Margin="8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="{Binding Status}" VerticalAlignment="Center"/>
                <ProgressBar Width="180" Height="16" IsIndeterminate="{Binding IsBusy}" Visibility="{Binding BusyVisibility}"/>
                <Button Content="Generate Script" Width="140" Command="{Binding GenerateScriptCommand}" />
                <Button Content="Deploy" Width="100" Command="{Binding DeployCommand}" />
                <Button Content="Save Report..." Width="120" Command="{Binding SaveReportCommand}" />
            </StackPanel>
        </DockPanel>
        Connections and options -->
    </Grid>
</UserControl>
