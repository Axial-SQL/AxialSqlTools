<UserControl x:Class="AxialSqlTools.SchemaCompareWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" MinWidth="900" MinHeight="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#FFDDDDDD" BorderThickness="0,0,0,1" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Margin="0,0,16,0" Orientation="Vertical">
                    <TextBlock Text="Source" FontWeight="Bold" Margin="0,0,0,4" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <Button Content="Use Selected" Width="120" Command="{Binding PickSourceCommand}" />
                        <Button Content="Clear" Width="70" Margin="8,0,0,0" Command="{Binding ClearSourceCommand}" />
                    </StackPanel>
                    <TextBlock Text="{Binding SourceDisplay}" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,16,0" Orientation="Vertical">
                    <TextBlock Text="Target" FontWeight="Bold" Margin="0,0,0,4" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <Button Content="Use Selected" Width="120" Command="{Binding PickTargetCommand}" />
                        <Button Content="Clear" Width="70" Margin="8,0,0,0" Command="{Binding ClearTargetCommand}" />
                    </StackPanel>
                    <TextBlock Text="{Binding TargetDisplay}" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="Swap" Width="80" Margin="0,0,8,0" Command="{Binding SwapConnectionsCommand}" />
                    <Button Content="Cancel" Width="120" Margin="0,0,8,0" Command="{Binding CancelCompareCommand}"
                            Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Button Content="Compare" Width="120" Command="{Binding CompareCommand}" />
                </StackPanel>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" Margin="8">
            <TabItem Header="Differences">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*" />
                        <RowDefinition Height="3*" />
                    </Grid.RowDefinitions>

                    <DataGrid Grid.Row="0" ItemsSource="{Binding Differences}" AutoGenerateColumns="False" IsReadOnly="True"
                              SelectedItem="{Binding SelectedDifference, Mode=TwoWay}" CanUserAddRows="False"
                              HeadersVisibility="Column" Margin="0,0,0,8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                            <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="*" />
                            <DataGridTextColumn Header="Difference Type" Binding="{Binding DifferenceType}" Width="*" />
                            <DataGridTextColumn Header="Source Object" Binding="{Binding SourceObject}" Width="4*" />
                            <DataGridTextColumn Header="Target Object" Binding="{Binding TargetObject}" Width="4*" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <GroupBox Margin="0,0,4,0">
                            <GroupBox.Header>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Source Definition" VerticalAlignment="Center" />
                                    <Button Grid.Column="1" Content="Copy" Width="80" Margin="8,0,0,0"
                                            Command="{Binding CopySourceDefinitionCommand}" />
                                </Grid>
                            </GroupBox.Header>
                            <Grid>
                                <ListView ItemsSource="{Binding SourceDiffLines}" BorderThickness="0" FontSize="12"
                                          ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
                                          VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.ScrollUnit="Pixel">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Padding" Value="0" />
                                            <Setter Property="FontFamily" Value="Consolas" />
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DiffType}" Value="Inserted">
                                                    <Setter Property="Background" Value="#FFE1FFE1" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DiffType}" Value="Deleted">
                                                    <Setter Property="Background" Value="#FFF7D6D6" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DiffType}" Value="Modified">
                                                    <Setter Property="Background" Value="#FFE9F3FF" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Opacity="{Binding Opacity}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="24" />
                                                    <ColumnDefinition Width="50" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding Indicator}" HorizontalAlignment="Center" />
                                                <TextBlock Grid.Column="1" Text="{Binding LineNumber}" Foreground="#FF888888" HorizontalAlignment="Right"
                                                           Margin="0,0,8,0" />
                                                <TextBlock Grid.Column="2" Text="{Binding Text}" TextWrapping="NoWrap"
                                                           FontFamily="Consolas" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                                <TextBlock Text="Select a difference to preview the source definition." Foreground="#FF666666"
                                           FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedDifference}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </GroupBox>

                        <GroupBox Grid.Column="1" Margin="4,0,0,0">
                            <GroupBox.Header>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Target Definition" VerticalAlignment="Center" />
                                    <Button Grid.Column="1" Content="Copy" Width="80" Margin="8,0,0,0"
                                            Command="{Binding CopyTargetDefinitionCommand}" />
                                </Grid>
                            </GroupBox.Header>
                            <Grid>
                                <ListView ItemsSource="{Binding TargetDiffLines}" BorderThickness="0" FontSize="12"
                                          ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
                                          VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.ScrollUnit="Pixel">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Padding" Value="0" />
                                            <Setter Property="FontFamily" Value="Consolas" />
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DiffType}" Value="Inserted">
                                                    <Setter Property="Background" Value="#FFE1FFE1" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DiffType}" Value="Deleted">
                                                    <Setter Property="Background" Value="#FFF7D6D6" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DiffType}" Value="Modified">
                                                    <Setter Property="Background" Value="#FFE9F3FF" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Opacity="{Binding Opacity}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="24" />
                                                    <ColumnDefinition Width="50" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding Indicator}" HorizontalAlignment="Center" />
                                                <TextBlock Grid.Column="1" Text="{Binding LineNumber}" Foreground="#FF888888" HorizontalAlignment="Right"
                                                           Margin="0,0,8,0" />
                                                <TextBlock Grid.Column="2" Text="{Binding Text}" TextWrapping="NoWrap"
                                                           FontFamily="Consolas" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                                <TextBlock Text="Select a difference to preview the target definition." Foreground="#FF666666"
                                           FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedDifference}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </GroupBox>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Deployment Script">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,4">
                        <Button Content="Copy Script" Width="120" Command="{Binding CopyDeploymentScriptCommand}" />
                    </StackPanel>
                    <TextBox Grid.Row="1" Text="{Binding DeploymentScript, Mode=OneWay}" FontFamily="Consolas" FontSize="12" AcceptsReturn="True"
                             TextWrapping="NoWrap" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                             IsReadOnly="True" />
                </Grid>
            </TabItem>
        </TabControl>

        <Border Grid.Row="2" BorderBrush="#FFDDDDDD" BorderThickness="1,1,1,0" Padding="8">
            <DockPanel>
                <TextBlock Text="{Binding Status}" VerticalAlignment="Center" />
                <ProgressBar Width="180" Height="16" Margin="12,0,0,0" IsIndeterminate="True"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
