<Window x:Class="AxialSqlTools.DataCompare.SqlConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SQL Server connection" Width="490" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource AxialThemeBackgroundBrush}">
    <Window.Resources>
        <ResourceDictionary><ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="/AxialSqlTools;component/Themes/SharedToolWindowTheme.xaml"/>
        </ResourceDictionary.MergedDictionaries></ResourceDictionary>
    </Window.Resources>
    <Grid Margin="18">
        <Grid.ColumnDefinitions><ColumnDefinition Width="135"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Server" VerticalAlignment="Center"/>
        <TextBox x:Name="ServerBox" Grid.Column="1" Margin="0,4" MinHeight="26" AutomationProperties.Name="SQL Server name"/>
        <TextBlock Grid.Row="1" Text="Database" VerticalAlignment="Center"/>
        <TextBox x:Name="DatabaseBox" Grid.Row="1" Grid.Column="1" Margin="0,4" MinHeight="26" Text="master"/>
        <TextBlock Grid.Row="2" Text="Authentication" VerticalAlignment="Center"/>
        <ComboBox x:Name="AuthenticationBox" Grid.Row="2" Grid.Column="1" Margin="0,4" SelectedIndex="0" SelectionChanged="AuthenticationChanged">
            <ComboBoxItem Content="Windows authentication"/><ComboBoxItem Content="SQL Server authentication"/>
            <ComboBoxItem Content="Microsoft Entra interactive"/><ComboBoxItem Content="Microsoft Entra default"/>
        </ComboBox>
        <TextBlock Grid.Row="3" Text="User name" VerticalAlignment="Center"/>
        <TextBox x:Name="UsernameBox" Grid.Row="3" Grid.Column="1" Margin="0,4" MinHeight="26" IsEnabled="False"/>
        <TextBlock Grid.Row="4" Text="Password" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Grid.Column="1" Margin="0,4" MinHeight="26" IsEnabled="False"/>
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Margin="0,10">
            <CheckBox x:Name="EncryptBox" Content="Encrypt connection" IsChecked="True" Margin="0,3"/>
            <CheckBox x:Name="TrustBox" Content="Trust server certificate" Margin="0,3"/>
        </StackPanel>
        <TextBlock Grid.Row="6" Grid.ColumnSpan="2" TextWrapping="Wrap" Margin="0,4,0,14"
                   Text="The connection is used for this comparison window. Credentials are not saved."/>
        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Connect" IsDefault="True" Click="ConnectClick" MinWidth="85" Margin="4,0"/>
            <Button Content="Cancel" IsCancel="True" MinWidth="85" Margin="4,0"/>
        </StackPanel>
    </Grid>
</Window>
