<UserControl x:Class="AxialSqlTools.HealthDashboard_ServersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="1000"
             Name="MyToolWindow"
             Background="{DynamicResource AxialThemeBackgroundBrush}"
             Foreground="{DynamicResource AxialThemeForegroundBrush}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/AxialSqlTools;component/Themes/SharedToolWindowTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{DynamicResource AxialThemeBackgroundBrush}">
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Background="{DynamicResource AxialThemeHeaderBackgroundBrush}">
                <TextBlock Margin="5" HorizontalAlignment="Center" VerticalAlignment="Top"
                           FontWeight="Bold" FontSize="18">Health Dashboard - Servers</TextBlock>
                <TextBlock Margin="10" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Run Text="Feature description in"/>
                    <Run Text=" "/>
                    <Hyperlink NavigateUri="https://github.com/Axial-SQL/AxialSqlTools/wiki/Server-Health-Dashboard"
                               RequestNavigate="WikiLink_RequestNavigate">
                        <Run Text="Wiki"/>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>

            <DataGrid x:Name="MyDataGrid" AutoGenerateColumns="False" Height="220" Margin="5"
                      Style="{DynamicResource AxialThemedDataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Column 1" Binding="{Binding Property1}" Width="200" />
                    <DataGridTextColumn Header="Column 2" Binding="{Binding Property2}" Width="200" />
                    <!-- Add more columns as needed -->
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>
    </Grid>
</UserControl>
